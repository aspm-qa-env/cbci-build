apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: prod
on:
  workflow_dispatch:

jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: SVMultiLangRepo
          artifact-id: 45b8e4c9-0c10-4a14-9b7d-ec646cf745d7
          target-environment: QA